int main() {
    int a = 5;
    int b = 10;

    // Repeated subexpressions
    int c = (a + b) * (a - b);
    printInt(c);
    int d = a - b + 1;
    printInt(d);
    printInt((a + b) + (a - b));
    a = 3;
    printInt((a + b) + (a - b));
    int e = a - b;
    printInt(e);
    return 0;
}
